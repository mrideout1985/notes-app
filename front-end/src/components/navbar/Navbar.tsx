import styles from "./Navbar.module.scss"

const Navbar = () => {
	return (
		<nav className={styles.navbar}>
			<div className={styles.button_group}>Navbar</div>
		</nav>
	)
}

export default Navbar
